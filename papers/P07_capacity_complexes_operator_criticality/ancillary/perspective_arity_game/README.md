# Perspective Arity Game

This branch corrects the semantic index without changing the preserved
version-4 research:

\[
P_0=\text{no distinction},\qquad
P_1=\text{native binary distinction},\qquad
P_4=\text{hypothetical atomic four-resultant distinction}.
\]

It is not a cellular automaton and does not modify Conway's Game of Life. It
is an exhaustive finite source-closure generated by one atomic relation.

Run:

```bash
python -m unittest discover -v
python census.py --max-events 3 --output audit.json
python refinement_audit.py --max-arity 6 --output refinement_audit.json
python interaction_audit.py --max-arity 8 --max-events 8 --output interaction_audit.json
python native_extension_audit.py --arity 4 --max-events 4 --output native_extension_audit.json
python capacity_audit.py --arity 4 --max-events 4 --output capacity_audit.json
python groupoid_lift_audit.py --arity 4 --max-events 4 --output groupoid_lift_audit.json
python contextual_distinction_audit.py --arity 4 --max-events 4 --output contextual_distinction_audit.json
python binary_capacity_audit.py --max-bit 8 --sample-limit 4096 --output binary_capacity_audit.json
python binary_identity_audit.py --max-bits 14 --output binary_identity_audit.json
python prime_arity_audit.py --max-arity 200 --sample-limit 1000 --output prime_arity_audit.json
python arithmetic_emergence_audit.py --limit 10000 --output arithmetic_emergence_audit.json
python cross_boundary_audit.py --output cross_boundary_audit.json
python uncollapsed_fibre_audit.py --output uncollapsed_fibre_audit.json
```

`FOUNDATION.md` states the boundary. `THEOREMS.md` proves the complete
binary-presentation fiber and both descent criteria. `FINDINGS.md` reports
the verified run. `REFINEMENT_THEOREMS.md` derives the first complete-fiber
invariant and its zero law. `ARITHMETIC_COMPARISON.md` performs the
post-freeze comparison with the classical arithmetic structure.

`INTERACTION_THEOREMS.md` continues the source through interacting atomic
occurrences. It derives the raw interaction operator, proves and then correctly
limits its arity-four stability effect, removes the accounting assumption, and
constructs the native extension inverse and component factorization.

`CAPACITY_THEOREMS.md` retains every binary cost presentation of the connected
irreducibles and proves the convergence/nonvanishing half-plane common to the
complete code fiber.

`FIBER_INVOLUTION.md` derives the exact finite-horizon character inversion,
its forced completion, and the common reciprocal-zero axis while isolating the
remaining infinite-limit obstruction.

`SYMMETRY_REALIZATION.md` performs the next accounting check. It proves that
geometric Euler factors arise only after taking one copy of every isomorphism
class, while retaining all labelled identities and symmetries gives an
exponential component law. The earlier normalized central axis is therefore
retained as a valid projection effect but not promoted to the source.

`CONTEXTUAL_DISTINCTION.md` derives the observation fibres generated by the
extension relation itself. One additional interaction separates all
past-indistinguishable connected forms at ranks two and three without changing
the underlying forms. A general disjoint-context theorem proves the same for
every finite rank; the two rank-four pairs unresolved by the finite table are
therefore truncation effects.

`BINARY_CAPACITY_CHARACTER.md` applies the unique nontrivial binary sign to
the symmetry carrier. At power-of-two arities, incidence-capacity parities are
exactly the binary digits of the resultant coordinate and force the generated
differential law \(F_q^{(q)}+F_q=0\).

The same document then retains all capacity digits at once, producing the
self-similar identity carrier
\(T(z)=\prod_{j\ge0}(1-z^{2^j})\) and its generated natural boundary.

`PRIME_ARITY.md` proves that the arities whose intermediate capacities vanish
in their own residue system are exactly the primes. At each such arity, the
same capacities reconstruct base-prime digits and generate a complete fibre
of self-similar digit characters.

`ARITHMETIC_EMERGENCE.md` composes the transparent coordinates only after they
have emerged. Their additive binary cost is \(\log_2n\), their independent
selection inverse is the arithmetic Möbius function, and their scalar cost
character is the zeta Dirichlet/Euler product in \(\Re s>1\).

`CROSS_BOUNDARY_COMPLETION.md` corrects the earlier diagonal character family
to the full multi-place fibre. Its complete least-place traces are entire;
their cross-prime compatibility constructs the meromorphic continuation with
only the pole at one. Binary complete differences give a globally convergent
zero equation and force the negative even zeros. The same derivation proves
that the collapsed prime traces have analytic rank one, so their interaction
alone cannot force a critical line.

`UNCOLLAPSED_FIBRE_DUALITY.md` retains all character coordinates and lets unit
advance couple them through carry. Diagonalizing that interaction produces
the cyclic carry characters and an exact sine-square distinction spectrum.
Its uniquely nondegenerate scaling and the previously derived exponential
lift generate the Gaussian/theta carrier. Fourier completeness then derives
theta duality; its scale character makes the gamma factor and zeta reflection
law emerge rather than importing them. The resulting fourfold zero orbit is
proved, along with the exact reason reflection alone does not establish RH.

The JSON audit files contain the exact finite results.
