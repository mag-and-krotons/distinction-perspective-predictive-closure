# Archived predecessor ledgers

`PAPER_INDEX_V7_INCOMPLETE_PREDECESSOR.csv` is retained only as a historical artifact. It contains one row and must not be used as the release index. The authoritative release-8 index is `../PAPER_INDEX.csv`.
